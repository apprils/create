{
  "devPort": {{devPort}},
  "apiPort": {{apiPort}},
  "scripts": {
    "vite": "node --env-file=../.env --trace-deprecation $(which vite) $*",
    "dev": "npm run vite",
    "build": "npm run vite build",
    "//✨ trailing entry": ""
  }
}

